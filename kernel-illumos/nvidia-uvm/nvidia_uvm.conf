#
# nvidia_uvm: NVIDIA Unified Memory pseudo driver, /dev/nvidia-uvm and
# /dev/nvidia-uvm-tools.
#
name="nvidia_uvm" parent="pseudo" instance=0;
#
# UVM module parameters are properties of the same name, read when UVM
# starts; see README.md.  For example:
#
# uvm_perf_prefetch_threshold=75;
# uvm_channel_gpfifo_loc="sys";
